# 大數據健康檢測 App — 介面設計文件

> **版本**：v2.0　**更新**：2026-05-22　**作者**：超艋健康團隊

---

## 目錄

1. [設計系統](#1-設計系統)
2. [頁面架構](#2-頁面架構)
3. [頁面詳細規格](#3-頁面詳細規格)
4. [元件庫](#4-元件庫)
5. [導覽邏輯](#5-導覽邏輯)
6. [動畫規格](#6-動畫規格)
7. [響應式設計](#7-響應式設計)

---

## 1. 設計系統

### 1.1 色彩

| 名稱 | 變數 | Hex | 用途 |
|------|------|-----|------|
| 暖奶油（背景） | `--bg-primary` | `#F7F2EA` | 全站底色 |
| 米色（次背景） | `--bg-secondary` | `#EDE5D6` | 卡片底、區塊 |
| 主金色 | `--gold-main` | `#C49A5A` | 按鈕、強調、icon |
| 淡金 | `--gold-light` | `#D9B882` | hover、邊框 |
| 深金 | `--gold-deep` | `#9E7A3F` | 標題文字、深色金 |
| 淡金底 | `--gold-pale` | `#F0E4CC` | 輸入框、分隔線 |
| 深棕文字 | `--text-dark` | `#2E2418` | 主標題 |
| 中棕文字 | `--text-mid` | `#6B5840` | 內文 |
| 柔棕文字 | `--text-soft` | `#A8916F` | 副標、說明 |
| 提示文字 | `--text-hint` | `#C4AE8E` | placeholder |
| 成功 | `--ok-color` | `#5A9E6B` | 完成、成功訊息 |
| 警告 | `--warn-color` | `#C49A5A` | 注意 |
| 錯誤 | `--alert-color` | `#C46A5A` | 錯誤、警示 |

### 1.2 字體

| 層級 | 字體 | 大小 | 粗細 | 用途 |
|------|------|------|------|------|
| 主標題 | Noto Serif TC | 22–26px | 700 | 頁面大標 |
| 副標題 | Noto Serif TC | 16–18px | 600 | 區塊標題 |
| 內文 | Noto Sans TC | 14–15px | 400 | 一般文字 |
| 說明 | Noto Sans TC | 12–13px | 300 | 說明、備注 |
| 數字強調 | Noto Sans TC | 28–36px | 700 | 分數、次數 |

### 1.3 圓角

| 變數 | 值 | 用途 |
|------|----|------|
| `--radius-lg` | 24px | 卡片、Modal |
| `--radius-md` | 16px | 按鈕、輸入框 |
| `--radius-sm` | 10px | 標籤、小元件 |

### 1.4 陰影

```
暖色陰影：rgba(180,140,90,0.15)   → 卡片浮起
深色陰影：rgba(100,70,30,0.12)    → 按鈕按壓
玻璃邊框：rgba(196,154,90,0.25)   → 毛玻璃元件
```

---

## 2. 頁面架構

```
App（單頁 SPA）
│
├── page-login          登入頁
├── page-main           首頁（健康任務）← 登入後預設落點
├── page-challenge      檢測頁（面舌診掃描）
├── page-report         報告頁
├── page-history        記錄頁
├── page-bottle         漂流瓶
├── page-achievement    成就頁
├── page-leaderboard    排行榜
└── page-admin          管理後台（管理員限定）

Modal 覆蓋層
├── code-modal          增加次數（推薦碼 / 付費）
├── payment-modal       付費方案選擇
├── forgot-modal        忘記密碼
└── share-friend-modal  推薦朋友
```

---

## 3. 頁面詳細規格

---

### 3.1 登入頁（`page-login`）

```
┌─────────────────────────────┐
│  背景圖 bg.png（全版）        │
│                              │
│  ┌──── 品牌區 ────┐          │
│  │  大數據健康檢測  │          │
│  │  ── 分隔線 ──   │          │
│  │   面舌診檢測    │          │
│  └────────────────┘          │
│                              │
│  ┌── 掃描裝飾圓環 ──┐         │
│  │   貓頭鷹 owl1.png │        │
│  │   角落掃描框      │        │
│  └──────────────────┘        │
│                              │
│  ┌── 登入卡片 ──────┐         │
│  │  Google 圖示說明  │        │
│  │  ☐ 我已閱讀並同意服務條款  │
│  │  [ 使用 Google 帳號登入 ] │
│  │  （未勾選條款→按鈕停用）   │
│  │  錯誤訊息欄                │
│  │  🔒 隱私保護說明           │
│  └──────────────────────────┘│
│                              │
│  超艋健康團隊                 │
│  🌐 查看官方網站 & 服務說明   │
└─────────────────────────────┘
```

**互動規則**
- 條款 checkbox 未勾選 → Google 按鈕 `disabled`（透明度 45%）
- LINE / FB WebView → 顯示警告橫幅，強制複製網址
- Google OAuth 回調後自動解析 token 並進入 首頁

---

### 3.2 首頁（`page-main`）— 健康任務

```
┌─────────────────────────────┐
│  大數據健康任務               │
│  每日打卡，積累健康資本        │
├─────────────────────────────┤
│  🐷 健康存錢筒                │
│  每完成一項任務，存入一枚健康幣 │
│                              │
│  ┌── 豬公插圖區（220×220） ──┐│
│  │  piggy.png（手繪水彩豬）  ││
│  │  + 金幣「H」疊加動畫       ││
│  │               [n/100]    ││
│  └──────────────────────────┘│
│                              │
│  ▓▓▓▓▓░░░░░░░░░░░░░░░  進度條 │
│  0    25    50    75   100   │
├─────────────────────────────┤
│  ┌── 14天健康任務卡片 ───────┐ │
│  │  ⚡ 改善腸胃健康—14天計劃  │ │
│  │  ✓ Day 1 早餐前喝一杯溫水 │ │
│  │  ▶ Day 2 飯後散步15分鐘  │ │  ← 今日（高亮）
│  │  🔒 Day 3 尚未解鎖        │ │
│  └──────────────────────────┘│
├─────────────────────────────┤
│  底部導覽列                   │
│  [首頁●] [檢測] [漂流瓶]      │
│  [成就] [記錄]                │
└─────────────────────────────┘
```

**豬公金幣規則**
- 全部歷史完成任務數 → 決定金幣數量（上限 100）
- 金幣從底部往上格狀堆疊（6 顆/排）
- 新完成 → `coinDrop` 動畫（由上方落入）

---

### 3.3 檢測頁（`page-challenge`）— 面舌診掃描

```
┌─────────────────────────────┐
│  大數據健康檢測               │
│  面舌診檢測                  │
│  [⏱ 剩餘 n 次 ＋添加]        │  ← 點開 code-modal
│  會員碼：1234567  [複製]      │
├─────────────────────────────┤
│  步驟指示器：①——②——③        │
├─────────────────────────────┤
│  Step 1：臉部上傳             │
│  ┌── 上傳區（掃描框動畫） ──┐  │
│  │  臉部輪廓 icon            │  │
│  │  「點擊上傳臉部照片」      │  │
│  └──────────────────────────┘│
│  [下一步 →]（上傳後顯示）     │
│                              │
│  Step 2：舌頭上傳（同上格式） │
│  Step 3：確認送出              │
│  ┌── AI 分析載入動畫 ──────┐  │
│  │  貓頭鷹掃描 + 旋轉圈     │  │
│  │  輪播文字提示             │  │
│  └──────────────────────────┘│
├─────────────────────────────┤
│  底部導覽列（檢測●高亮）      │
└─────────────────────────────┘
```

---

### 3.4 報告頁（`page-report`）

```
┌─────────────────────────────┐
│  健康分析報告　2026.05.22     │
├─────────────────────────────┤
│  ┌── 健康分數圓圈 ─────────┐  │
│  │       82                 │  │
│  │      / 100               │  │
│  └──────────────────────────┘│
│  ▓▓▓▓▓▓▓▓░░  得分進度條      │
├─────────────────────────────┤
│  ⚠ 主要風險提示              │
│  卡片 ×9：                   │
│  · 關鍵營養素建議             │
│  · 失調注意                  │
│  · 面部六區診斷               │
│  · 舌診五項分析               │
│  · 體質判斷                  │
│  · 食療建議                  │
│  · 推薦穴位保健               │
│  · 作息調整建議               │
│  · 西醫觀察提示               │
├─────────────────────────────┤
│  [ LINE 聯絡超艋健康團隊 ]    │
│  [ 下載報告 ] [ ← 重新診斷 ] │
└─────────────────────────────┘
```

---

### 3.5 增加次數 Modal（`code-modal`）

```
┌─── 增加檢測次數 ───────────┐
│  🤝 填入推薦碼              │
│  填入朋友的7位會員碼         │
│  每帳號限用一次，最多28次    │
│  [__輸入7位會員碼__________]│
│  [      確認填入           ]│ ← btn-gold
│  結果訊息                   │
│  ─────── 或 ───────        │
│  [   💳 選擇付費方案 →     ]│ ← 開 payment-modal
│  [         取消            ]│
└─────────────────────────────┘
```

---

### 3.6 付費 Modal（`payment-modal`）

**Step 1：選方案**
```
┌─── 購買檢測次數 ───────────┐
│  選擇方案，透過LINE Pay付款  │
│                             │
│  ┌── 單次體驗 ────────────┐ │
│  │  45 元 · 1次檢測        │ │
│  └─────────────────────────┘│
│  ┌── 三次套餐 🔥最受歡迎 ──┐ │
│  │  120 元 · 3次 (40/次)   │ │
│  └─────────────────────────┘│
│  ┌── 十次暢享 ─────────────┐ │
│  │  399 元 · 10次 (39.9/次)│ │
│  └─────────────────────────┘│
│  ╔═ 購買條款 ══════════════╗ │
│  ║ 購買前請詳閱服務條款…   ║ │
│  ╚════════════════════════╝ │
│  [         取消            ]│
└─────────────────────────────┘
```

**Step 2：LINE Pay 說明**
```
┌─── LINE Pay 付款 ──────────┐
│  三次套餐（3次）  NT$ 120元  │
│  ① 點下方按鈕開啟LINE客服    │
│  ② 告知方案名稱與帳號信箱    │
│  ③ 依指示完成LINE Pay轉帳   │
│  ④ 次數5分鐘內生效          │
│  [ LINE 開啟客服付款 ]       │ ← 綠色
│  [← 返回選擇方案]           │
└─────────────────────────────┘
```

---

## 4. 元件庫

### 4.1 按鈕

| 元件 class | 外觀 | 用途 |
|-----------|------|------|
| `.btn-gold` | 金色實底，白字 | 主要操作 |
| `.btn-ghost` | 透明底，金框，金字 | 次要操作 |
| `.btn-outline` | 白底，金框 | 第三層操作 |
| `.btn-google-main` | 白底，Google 色彩圖示 | Google 登入 |
| `.btn-line` | 綠底（#06C755），LINE 圖示 | LINE 相關 |
| `.btn-restart` | 淡底，文字 | 重新診斷 |

### 4.2 卡片

| 元件 class | 說明 |
|-----------|------|
| `.rcard` | 報告區塊卡片（白底，金邊） |
| `.task-card` | 任務列表卡片 |
| `.partner-card` | 夥伴連結卡片（已保留 ID 供 JS 用） |
| `.med-card` | 歷史記錄卡片 |
| `.pay-plan-card` | 付費方案卡片 |

### 4.3 輸入框

```css
.modal-input   /* Modal 內輸入框，高度 48px，金色 focus 邊框 */
.partner-inp   /* 夥伴碼輸入，寬度自動 */
.bottle-textarea /* 漂流瓶文字區塊，min-height 100px */
```

### 4.4 徽章／標籤

```
.badge-ok      → 綠色「正常」
.badge-warn    → 橙色「輕微失調」
.badge-alert   → 紅色「需注意」
.credit-badge  → 金色次數徽章（點擊開 code-modal）
```

### 4.5 存錢豬公元件

```
.piggy-bank-section   → 整體容器（padding 16px）
.piggy-img-wrap       → 220×220 相對定位容器
.piggy-img            → piggy.png，drop-shadow
.piggy-coins-overlay  → 絕對定位金幣覆蓋層
.piggy-coin           → 26×26 金幣圓點，中間「H」字
.piggy-count-badge    → 右下角 n/100 浮層
.piggy-bar            → 進度條軌道
.piggy-bar-fill       → 金色漸層填充（JS 控制 width%）
```

---

## 5. 導覽邏輯

### 5.1 底部導覽列結構

```
首頁 | 檢測 | 漂流瓶 | 成就 | 記錄 | [後台]
```

| 標籤 | 目標頁 | 觸發函式 |
|------|--------|---------|
| 首頁 | `page-main` | `showPage('page-main'); loadChallenge()` |
| 檢測 | `page-challenge` | `showPage('page-challenge'); resetToStep1()` |
| 漂流瓶 | `page-bottle` | `showPage('page-bottle'); loadBottle()` |
| 成就 | `page-achievement` | `showPage('page-achievement'); loadAchievement()` |
| 記錄 | `page-history` | `showPage('page-history'); loadHistory()` |
| 後台 | `page-admin` | 管理員限定，`loadAdminUsers()` |

### 5.2 Auth 路由邏輯

```
登入
  │
  ├── 管理員（phone = 0912345678 & name = PTGM，或管理員信箱）
  │       └→ page-admin
  │
  └── 一般使用者
          └→ page-main（首頁，觸發 loadChallenge）
```

### 5.3 showPage() 特殊處理

```javascript
// 切換到檢測頁 → 更新次數 + 重設步驟
if (pageId === 'page-challenge') {
  credit-num.textContent = credits
  setTimeout(resetToStep1, 50)
}

// 切換到首頁 → 載入任務與豬公
if (pageId === 'page-main') {
  setTimeout(loadChallenge, 100)
}
```

---

## 6. 動畫規格

| 動畫名稱 | 元件 | 效果 | 時長 |
|---------|------|------|------|
| `pageEnter` | `.page` | 淡入 + 上移 8px | 0.35s ease |
| `coinDrop` | `.piggy-coin` | 由上 40px 落入，scale 0.6→1 | 0.5s cubic-bezier |
| 上傳區脈衝 | `.upload-ring-anim` | 環形擴散 | 2.5s loop |
| 掃描線 | `.scan-line` | 上下掃描 | 2s loop |
| 旋轉圈 | `.loading-ring-1/2` | 順/逆時針旋轉 | 2/3s loop |
| 輪播文字 | `#carousel-track` | 橫向滑動 | JS 控制 |

---

## 7. 響應式設計

### 最大寬度

```css
.page         → max-width: 480px; margin: auto  (手機優先)
.modal-box    → max-width: 360px
.piggy-img-wrap → 220×220px 固定
```

### iOS 安全區處理

```css
padding-bottom: env(safe-area-inset-bottom, 0)  /* 底部 home bar */
```

### WebView 防護

偵測 LINE / FB / Instagram / WeChat 內建瀏覽器 → 自動顯示警告橫幅，停用 Google 登入，提示複製網址到 Safari/Chrome。

---

## 附錄：檔案結構

```
facialmonitor/
├── index.html          主應用（單頁 SPA）
├── about.html          官方網站／條款頁
├── css/
│   └── style.css       全站樣式（2500+ 行）
├── js/
│   ├── config.js       Supabase 設定、全域狀態
│   ├── auth.js         認證、路由、頁面切換
│   ├── analyze.js      圖片上傳、Edge Function 呼叫
│   ├── features.js     任務、豬公、推薦、付費、成就
│   ├── report.js       報告渲染
│   ├── admin.js        管理後台
│   └── utils.js        工具函式、WebView 偵測
├── piggy.png           Stitch 生成手繪水彩豬公插圖
├── owl1.png            登入頁裝飾圖
├── owl2.png            分析載入動畫圖
├── bg.png              登入頁背景
└── .github/
    └── workflows/
        └── deploy.yml  GitHub Actions 手動部署
```

---

*文件由 Claude Code 自動產生・如有更新請同步修改本文件*
